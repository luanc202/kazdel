import "htmx.org";

// You can add custom Alpine.js, DaisyUI JS components, or your own logic here
document.addEventListener('htmx:load', function (evt) {
    console.log("HTMX loaded new content");
});
